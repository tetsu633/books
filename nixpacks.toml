[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm ci", "npx prisma generate --schema=./libs/prisma/prisma/schema.prisma", "npx nx build backend"]

[start]
cmd = "node apps/backend/dist/main.js"